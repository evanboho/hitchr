<% provide :title, "edit ride" %>

<h2>Edit ride</h2>
<% @ride = Ride.find(1) %>
<%= form_for(@ride) do |f| %>
  <%= render 'shared/ride_form', :f => f %>
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>